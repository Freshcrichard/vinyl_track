import { capitalize } from '../../../helpers/capitalize';
import { test } from 'qunit';

// Replace this with your real tests.
test('it works', function(assert) {
  assert.equal(capitalize('it works'), 'It Works');
});
